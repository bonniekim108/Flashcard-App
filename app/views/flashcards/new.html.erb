<%= form_for @flashcard do |f| %>

 <%= f.label :word %><%= f.text_field :word %>
 <%= f.label :pronunciation %><%= f.text_field :pronunciation %>
 <%= f.label :definition %><%= f.text_field :definition %>
 <%= f.label :notes %><%= f.text_area :notes %>
 <%= f.submit "Add to flashcard" %>
<% end %>

<%= link_to "Show flashcards", flashcards_path %>
